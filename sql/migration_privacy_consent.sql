-- Миграция: фиксация согласия на обработку персональных данных при регистрации.
-- По ст. 9 152-ФЗ оператор должен уметь подтвердить факт получения согласия —
-- поэтому храним не только факт, но и момент времени.
use nexa;

ALTER TABLE `users`
  ADD COLUMN `privacy_consented_at` DATETIME NULL AFTER `created_at`;
